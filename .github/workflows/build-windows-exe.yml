name: Build Windows EXE

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller
      - name: Syntax check
        run: |
          python -m py_compile app.py
          python -m py_compile enhancements.py
          python -m py_compile launcher.py
      - name: Build EXE
        run: pyinstaller --noconfirm --clean --onefile --windowed --collect-all tkinterdnd2 --name BatchWatermarkToolProV4_1 launcher.py
      - name: Upload Windows EXE
        uses: actions/upload-artifact@v4
        with:
          name: BatchWatermarkToolProV4_1-Windows
          path: dist/BatchWatermarkToolProV4_1.exe
          if-no-files-found: error
